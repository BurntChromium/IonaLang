# String functions and data types for Iona

# `String` represents a buffer of bytes (C chars) with a fat pointer
struct String {
    data: Array<Byte>,
    len: Size,
    capacity: Size

    @metadata {
        Is: Export;
        Derives: Eq;
    }
}