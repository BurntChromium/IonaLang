#! String functions and data types for Iona

#! `String`s are dynamically sized buffers of bytes (`char`s in C)
struct String {
    data: Array<Byte>

    @metadata {
        Is: Export;
        Derives: Eq;
    }
}