#include <stdio.h>
#include "gen_strings.h"

/// @brief Print a raw C string to the console
/// @param string the input string 
void print_raw(char* string) {
    printf("%s\n", string);
}

/// @brief Print an Iona string to the console
/// @param string 
void print(String string) {
    
}