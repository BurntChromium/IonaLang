import fs;
import math with pow sqrt;

alias Salary = int;
alias Salaries = List[Salary];

struct Employee = id int :: salary Salary 
	is Public
	derives Log;

enum EmployeeType = Salaried int | Contract
	is Public
	derives Log;

fn foo = a int :: b int -> int {
	Is: Public;
	In: gt b 0;

	return div (add a 5) b;
}